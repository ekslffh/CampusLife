<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.calendar.dao.CalendarDao">
	<!-- 캘린더 일정 등록 -->
	<insert id="insert" parameterType="kr.or.ddit.calendar.vo.CalendarVO">
	INSERT INTO CALENDAR(
	cal_id
    , cal_title
    , cal_content
    , cal_start_dt
    , cal_end_dt
	) VALUES(
	CAL_NO_SEQ.NEXTVAL
	, #{calTitle,jdbcType=VARCHAR}
	, #{calContent,jdbcType=VARCHAR}
	, #{calStartDt,jdbcType=DATE}
	, #{calEndDt,jdbcType=DATE}
	)
	</insert>
	
	<!-- DB에 저장된 일정 가져오기 -->
	<select id="selectAll" parameterType="kr.or.ddit.calendar.vo.CalendarVO">
		SELECT 
		CAL_TITLE, CAL_CONTENT
		, CAL_START_DT, CAL_END_DT
		FROM CALENDAR
	</select>
</mapper>